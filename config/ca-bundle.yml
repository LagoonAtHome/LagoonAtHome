apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: public-bundle-lagoon-test-ca
spec:
  sources:
  - useDefaultCAs: true
  - secret:
      name: lagoon-local-secret
      key: "ca.crt"
  target:
    configMap:
      key: "ca-certificates.crt"
